# Database Schema

## Overview

PostgreSQL database accessed via Drizzle ORM. All schemas are in `db/schema/`. Migrations are in `db/migrations/`.

All IDs are **cuid2** strings (from `@paralleldrive/cuid2`), except `ticket_number` which is a PostgreSQL serial integer.

---

## Tables

### `user` (managed by Better Auth)

Stores agents and admins only. Customers are not users.

```
user
├── id                text PK (cuid2, generated by Better Auth)
├── email             text NOT NULL UNIQUE
├── name              text NOT NULL
├── email_verified    boolean NOT NULL DEFAULT false
├── image             text                         ← storage key for avatar, nullable
├── role              text                         ← 'agent' | 'admin' | null
├── banned            boolean NOT NULL DEFAULT false
├── ban_reason        text
├── ban_expires       timestamp with time zone
├── created_at        timestamp with time zone NOT NULL DEFAULT NOW()
└── updated_at        timestamp with time zone NOT NULL DEFAULT NOW()
```

### `session` (managed by Better Auth)

```
session
├── id                text PK
├── user_id           text NOT NULL → user.id (CASCADE DELETE)
├── token             text NOT NULL UNIQUE
├── expires_at        timestamp with time zone NOT NULL
├── ip_address        text
├── user_agent        text
├── impersonated_by   text
├── created_at        timestamp with time zone NOT NULL DEFAULT NOW()
└── updated_at        timestamp with time zone NOT NULL DEFAULT NOW()
```

### `account` (managed by Better Auth)

```
account
├── id                text PK
├── user_id           text NOT NULL → user.id (CASCADE DELETE)
├── provider_id       text NOT NULL    ← 'google' | 'magic-link'
├── account_id        text NOT NULL
├── access_token      text
├── refresh_token     text
├── ...               (standard Better Auth fields)
├── created_at        timestamp with time zone NOT NULL DEFAULT NOW()
└── updated_at        timestamp with time zone NOT NULL DEFAULT NOW()
```

### `verification` (managed by Better Auth)

```
verification
├── id                text PK
├── identifier        text NOT NULL    ← email address
├── value             text NOT NULL    ← hashed magic link token
├── expires_at        timestamp with time zone NOT NULL
├── created_at        timestamp with time zone NOT NULL DEFAULT NOW()
└── updated_at        timestamp with time zone NOT NULL DEFAULT NOW()
```

---

### `ticket_statuses`

Admin-configurable ticket statuses. The `slug` is the stable identifier referenced by `tickets.status`.

```
ticket_statuses
├── id                text PK (cuid2)
├── slug              text NOT NULL UNIQUE    ← immutable identifier, stored in tickets.status
├── label             text NOT NULL           ← display name (e.g. "Open", "In Progress")
├── color             text NOT NULL           ← color preset name (blue/amber/slate/red/…)
├── sort_order        integer NOT NULL DEFAULT 0
├── is_default        boolean NOT NULL DEFAULT false   ← exactly one row should be true
├── is_closed_state   boolean NOT NULL DEFAULT false   ← sets closedAt when ticket moves to this status
├── created_at        timestamp with time zone NOT NULL DEFAULT NOW()
└── updated_at        timestamp with time zone NOT NULL DEFAULT NOW()
```

### `ticket_categories`

Admin-configurable ticket categories. The `slug` is the stable identifier referenced by `tickets.category`.

```
ticket_categories
├── id                text PK (cuid2)
├── slug              text NOT NULL UNIQUE    ← immutable identifier, stored in tickets.category
├── label             text NOT NULL           ← display name (e.g. "Bug", "Billing")
├── color             text NOT NULL           ← color preset name
├── sort_order        integer NOT NULL DEFAULT 0
├── created_at        timestamp with time zone NOT NULL DEFAULT NOW()
└── updated_at        timestamp with time zone NOT NULL DEFAULT NOW()
```

---

### `api_keys`

Caller identity for the public API (`app/api/v1/*`) — a third auth mode
alongside agent/admin sessions and customer ticket tokens. Managed from
`/admin/api-keys`. See `docs/api.md` for the API itself.

```
api_keys
├── id                text PK (cuid2)
├── name              text NOT NULL           ← admin-chosen label, e.g. "Marketing site"
├── key_prefix        text NOT NULL           ← first 16 chars, shown in the admin UI
├── key_hash          text NOT NULL UNIQUE    ← sha256 of the full key; the raw key is never stored
├── created_by_id     text → user.id (SET NULL on delete), nullable
├── created_by_name   text NOT NULL
├── last_used_at      timestamp with time zone, nullable
├── revoked_at        timestamp with time zone, nullable   ← soft revoke, row is kept for audit history
└── created_at        timestamp with time zone NOT NULL DEFAULT NOW()
```

### `tickets`

Core ticket entity.

```
tickets
├── id                text PK (cuid2)
├── ticket_number     serial NOT NULL UNIQUE    ← auto-increment human ID (#1001+)
├── subject           text NOT NULL
├── description       text NOT NULL
├── category          text NOT NULL             ← slug from ticket_categories (e.g. 'bug')
├── status            text NOT NULL DEFAULT 'open'   ← slug from ticket_statuses (e.g. 'open')
├── priority          text NOT NULL DEFAULT 'normal' ← slug from ticket_priorities
├── customer_name     text NOT NULL
├── customer_email    text NOT NULL
├── customer_token    text NOT NULL UNIQUE (cuid2)   ← per-ticket secret for customer access
├── assigned_agent_id text → user.id (SET NULL on delete), nullable
├── awaiting_reply    boolean NOT NULL DEFAULT false ← true when the latest public message is from the customer
├── pending_replies   integer NOT NULL DEFAULT 0     ← customer messages since the last agent reply
├── source            text NOT NULL DEFAULT 'portal' ← 'portal' | 'api'
├── api_key_id        text → api_keys.id (SET NULL on delete), nullable   ← set when source = 'api'
├── closed_at         timestamp with time zone, nullable
├── created_at        timestamp with time zone NOT NULL DEFAULT NOW()
└── updated_at        timestamp with time zone NOT NULL DEFAULT NOW()

Indexes:
- ticket_number (unique)
- customer_email (for "my tickets" lookup)
- status
- assigned_agent_id
- created_at
- awaiting_reply
- priority
```

### `ticket_comments`

All replies, responses, and internal notes on a ticket.

```
ticket_comments
├── id                text PK (cuid2)
├── ticket_id         text NOT NULL → tickets.id (CASCADE DELETE)
├── author_id         text → user.id (SET NULL on delete), nullable   ← null for customer comments
├── author_name       text NOT NULL    ← stored at write time (preserved on author deletion)
├── author_role       text NOT NULL    ← 'customer' | 'agent' | 'admin'
├── content           text NOT NULL
├── is_internal       boolean NOT NULL DEFAULT false
├── created_at        timestamp with time zone NOT NULL DEFAULT NOW()
└── updated_at        timestamp with time zone NOT NULL DEFAULT NOW()

Indexes:
- ticket_id
- author_id
- is_internal
```

### `ticket_attachments`

Files attached to tickets or to comments.

```
ticket_attachments
├── id                text PK (cuid2)
├── ticket_id         text NOT NULL → tickets.id (CASCADE DELETE)
├── comment_id        text → ticket_comments.id (SET NULL on delete), nullable
│                          ← null = attached to ticket directly (not a comment)
├── filename          text NOT NULL    ← original filename for display
├── storage_key       text NOT NULL    ← e.g. tickets/{ticketId}/{uuid}/{filename}
├── file_size         integer NOT NULL ← bytes
├── mime_type         text NOT NULL
├── uploaded_by_id    text → user.id (SET NULL on delete), nullable   ← null for customers
├── uploaded_by_name  text NOT NULL    ← stored at write time
├── uploaded_by_role  text NOT NULL    ← 'customer' | 'agent' | 'admin'
└── created_at        timestamp with time zone NOT NULL DEFAULT NOW()

Indexes:
- ticket_id
- comment_id
```

### `ticket_activity`

Audit trail of all events on a ticket.

```
ticket_activity
├── id                text PK (cuid2)
├── ticket_id         text NOT NULL → tickets.id (CASCADE DELETE)
├── actor_id          text → user.id (SET NULL on delete), nullable   ← null for customer actions
├── actor_name        text NOT NULL    ← stored at write time
├── actor_role        text NOT NULL    ← 'customer' | 'agent' | 'admin' | 'system' | 'api'
├── action            text NOT NULL
│                     ← 'ticket_created' | 'status_changed' | 'assigned' | 'unassigned'
│                        'comment_added' | 'internal_note_added' | 'ticket_closed'
│                        'ticket_reopened' | 'attachment_added'
├── metadata          jsonb            ← e.g. { from: 'open', to: 'in_progress' }
└── created_at        timestamp with time zone NOT NULL DEFAULT NOW()

Indexes:
- ticket_id
- created_at
```

---

## Scaffold Tables (already exist)

These are provided by the KROVA scaffold and do not need to be created:

| Table | Purpose |
|-------|---------|
| `audit_logs` | Platform audit log (Better Auth admin actions) |
| `email_outbox` | Queued outbound emails (durable send) |
| `email_events` | Email delivery state tracking |
| `job_logs` | pg-boss job execution log |

---

## Schema File Organization

```
db/schema/
├── auth.ts            ← user, session, account, verification (Better Auth managed)
├── tickets.ts         ← tickets, ticket_comments, ticket_attachments, ticket_activity
├── ticket-config.ts   ← ticket_statuses, ticket_categories
├── api-keys.ts        ← api_keys
├── settings.ts        ← platform_settings
├── audit-logs.ts      ← audit_logs (scaffold)
├── email-outbox.ts    ← email_outbox, email_events (scaffold)
├── job-logs.ts        ← job_logs (scaffold)
└── index.ts           ← re-exports all tables for Drizzle adapter
```

---

## Drizzle Conventions

- Use `pgTable` from `drizzle-orm/pg-core`.
- Enums are stored as `text` with a comment or Zod enum for validation — not PostgreSQL `ENUM` type (makes migrations simpler).
- All timestamps use `timestamp({ withTimezone: true })`.
- All IDs use `text().primaryKey()` — no `serial` except for `ticket_number`.
- `updatedAt` is updated manually in every write operation — not via a database trigger.
- Foreign keys use `references(() => table.column, { onDelete: 'cascade' })` or `{ onDelete: 'set null' }` as appropriate.

---

## Migrations

```bash
# Generate migration after schema change
pnpm db:generate

# Apply pending migrations
pnpm db:migrate

# Push schema directly (dev only — skips migration file)
pnpm db:push

# Reset DB (drops all tables, re-migrates)
pnpm db:reset
```
